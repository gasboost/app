{
  "name": "@gasboost/app",
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.12",
    "@types/node": "^26.4.1",
    "vite": "^8.2.2"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -b tests/consumer &&  rm -rf tests/consumer/.build",
    "test": "vitest run"
  }
}
